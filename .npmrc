# @kiwi-health/capacitor-health-connect declares a stale @capacitor/core@^7 peer
# range but is compatible with Capacitor 8 (used here and in the LiFT app). Relax
# peer resolution so `npm install`/`npm ci` (CI) resolve consistently.
legacy-peer-deps=true
